<div id="bookmark_tag">
  <% if Bookmark.find_by(user_id: current_user.id, field_id: field.id) %>
    <%= render "unbookmark", field: @field %>
  <% else %>
    <%= render "bookmark",field: @field %>
  <% end %>
</div>
